version: 2.1

workflows:
  reports-build-test-deploy:
    jobs:
      - reports-verify:
          filters:
            branches:
              only:
                - /^(main.+|(?!main).*)$/

      - reports-deploy:
          filters:
            branches:
              only:
                - main

      - reports-send-test-coverage-reports:
          requires:
            - reports-verify
            - reports-deploy

#      - reports-update-ecs-service:
#          filters:
#            branches:
#              only:
#                - main
#          requires:
#            - reports-deploy

orbs:
  codecov: codecov/codecov@3.2.3
  aws-cli: circleci/aws-cli@1.3
  aws-ecs: circleci/aws-ecs@2.0

executors:
  reports-ubuntu-vm:
    machine:
      image: ubuntu-2204:2022.07.1
      docker_layer_caching: true
    working_directory: ~/complyt_work_directory/reports

  reports-openjdk-image:
    docker:
      - image: cimg/openjdk:11.0
    working_directory: ~/complyt_work_directory/reports

  reports-python-image:
    docker:
      - image: 'cimg/python:3.9.1'
    working_directory: ~/complyt_work_directory/reports

jobs:
  reports-verify:
    executor: reports-ubuntu-vm
    steps:
      - checkout:
          path: ~/complyt_work_directory
      - run:
          name: Execute
          command: ./mvnw clean verify

      - persist_to_workspace:
          root: target
          paths:
            - coverage-reports
            - failsafe-reports
            - surefire-reports
            - site

  reports-deploy:
    executor: reports-ubuntu-vm
    steps:
      - checkout:
          path: ~/complyt_work_directory
      - run:
          name: Avoid hosts unknown for github
          command: echo -e "Host github.com\n\tStrictHostKeyChecking no\n" > ~/.ssh/config

      - add_ssh_keys:
          fingerprints:
            - "d1:1b:cb:32:11:5d:0e:bc:67:c7:25:0f:ca:41:57:b1"
      - run:
          name: Setup Git credentials user email
          command: git config user.email "alexander@complyt.io"

      - run:
          name: Setup Git credentials user name
          command: git config user.name "Via CircleCI Build"

      - run:
          name: Creating Maven settings-security.xml file
          command: echo "<settingsSecurity><master>${MAVEN_SECURITY_MASTER}</master></settingsSecurity>" > ~/.m2/settings-security.xml

      - run:
          name: Release a Production Version
          command: ./mvnw --settings .circleci/settings.xml --batch-mode release:clean release:prepare release:perform

      - persist_to_workspace:
          root: target
          paths:
            - coverage-reports
            - failsafe-reports
            - surefire-reports
            - site

  reports-send-test-coverage-reports:
    executor: reports-openjdk-image
    steps:
      - checkout:
          path: ~/complyt_work_directory
      - attach_workspace:
          at: target
      - run:
          name: Save test results
          command: |
            mkdir -p test-results
            find . -type f -regex ".*/target/surefire-reports/.*xml" -exec cp {} test-results/ \;
          when: always
      - store_test_results:
          path: test-results/reports
      - codecov/upload:
          file: target/site/jacoco-ut/jacoco.xml
          token: CODECOV_TOKEN
          flags: reports

  reports-update-ecs-service:
    executor: reports-python-image
    steps:
      - aws-cli/setup:
          aws-access-key-id: AWS_ACCESS_KEY_ID
          aws-region: AWS_DEFAULT_REGION
          aws-secret-access-key: AWS_SECRET_ACCESS_KEY
      - aws-ecs/update-service:
          cluster-name: 'Complyt'
          container-image-name-updates: 'container=reports,tag=latest'
          family: 'complyt-td-production'
          service-name: 'complyt-srvc-production'