package com.complyt.v1.config.error_messages;

public interface DataConflictErrorMessages {
    
    String generic_message = "The requested operation failed because there was an unresolvable conflict between two or more inputs";
}
