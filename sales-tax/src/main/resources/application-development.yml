server:
  port: ${PORT:9898}

spring:
  data:
    mongodb:
      uri: mongodb+srv://${spring.datasource.username}:${spring.datasource.password}@cluster0.tlaie.mongodb.net/eyal

logging:
  level:
    root: DEBUG
    web: DEBUG
    org:
      springframework:
        security: DEBUG
    reactor:
      netty:
        http: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: "*"